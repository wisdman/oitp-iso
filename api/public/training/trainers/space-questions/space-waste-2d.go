package spaceQuestions

import (
	"github.com/wisdman/oitp-isov/api/lib/db"
)

func BuildWaste2D(
	sql *db.Transaction,
	complexity uint8,
) (
	configs []interface{},
	err error,
) {
	return configs, nil
}
